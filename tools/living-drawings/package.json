{
  "name": "hux-living-drawings",
  "private": true,
  "description": "Hux living drawings and 3D sandbox. The pages need no build; this only pins the physics engine for the headless checks.",
  "license": "MIT",
  "scripts": {
    "test": "node validation-test.js && node assumption-audit.js > /dev/null && node frontal.js > /dev/null && node sim-test.js"
  },
  "devDependencies": {
    "@dimforge/rapier3d-compat": "0.20.0"
  }
}
